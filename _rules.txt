// (По умолчанию) означает значение, которое используется когда параметр не указан, или указан с недопустимым значением.
// Любые не указанные параметры допустимы, но игнорируются
{
	// "standard" - голосование проводится если выставили больше одного игрока. (По умолчанию). 
	// "as_others" - голосование проводится точно также как и в остальных раундах. Сейчас такое правило нигде не применяется, но раньше было.
	// Правила по выставлению и убийству в нулевом раунде. Значения:
	// "no_kill" - голосование проводится как и в остальных раундах, но победитель не умирает, а только получает минуту поговорить. Такое применяется кое-где в новичковых играх. Просто чтобы не дать новичкам накосячить чрезмерно.
	"day0_voting": "standard" / "as_others" / "no_kill",

	// То самое пресловутое правило автокатастрофы на четверых. Значения:
	// false - нельзя. (По умолчанию). 
	// true - можно
	"crash4": false / true,

	// Экзотическое правило, которое я видел ровно в одном клубе. Там позволяют игроку убитому ночью в своем последнем слове утром выставлять игроков. Правило мне понравилось и мы даже практиковали его какое-то время в Ванкувере, но потом отменили, потому что редкое правило. Значения:
	// false - нельзя. (По умолчанию). 
	// true - можно
	"dead_nomination": false / true,

	// Что делать когда игрок ушел замечаниями днем до голосования. Правило борьбы с красными и черными монстрами. Тут клубы выступают кто во что горазд. Иногда такая экзотика встречается, что даже не сразу и разберешься. Значения:
	// "always" - если игрок ушел замечаниями, голосование отменяется. (По умолчанию). 
	// "never" - голосование проводится все равно. Клуб не борется с монстрами вообще.
	// "if_nominated" - голосование отменяется только когда игрок был выставлен, а если не выставлен, то все равно проводится. Работает против черных монстров, но для красных есть дырки.
	// "participation" - (разновидность предыдущего правила) голосование не отменяется но если ушедший игрок выставлен, то он остается в списке для голосования. Если стол проголосует против него, то никто больше не уйдет. А если против кого-то другого, то уйдут оба. Работает против черных монстров, но для красных есть дырки.
	"voiting_canceling": "always" / "never" / "if_nominated" / "participation"

	// Правила назначения лучшего игрока. Значения:
	// "any" - из любой команды.
	// "winner" - только из победившей команды. (По умолчанию).
	// "no" - вообще не назначается.
	"best_player": "any" / "winner" / "no"

	// Правила назначения лучшего хода. Значения:
	// "one" - только одному игроку.
	// "many" - можно дать нескольким игрокам. 
	// "no" - вообще не назначается. (По умолчанию).
	"best_move": "one" / "many" / "no"

	// Спрашивать ли у игрока убитого первой ночью трех мафиози. Значения:
	// true - да. (По умолчанию). 
	// false - нет.
	"guess_mafia": true / false

	// Если игрок получил три замечания, но еще не говорил в этот день, когда он пропускает слово?  Значения:
	// true - в тот же день. (По умолчанию).
	// false - на следующий день. Как ни странно это правило практикуют многие клубы. Во всяком случае, практиковали в 2012 году.
	"warn3_same_day": true / false

	// Если у игрока три замечания, а за столом 3 или 4 человека. Угадайка. Получит ли он слово? Значения:
	// true - да. 
	// "short" - укороченное. 30 сек. (По умолчанию).
	// false - нет.
	"warn3_crit": true / false / "short"

	// Когда умерли все, кто побеждает? Значения:
	// "civ" - город.
	// "maf" - мафия.
	// "draw" - ничья. (По умолчанию).
	// "impossible" - невозможно, голосование за подъем всех не проводится, все остаются.
	"all_dead": "civ" / "maf" / "draw"

	// Три раунда никто не погибает. Что происходит? Значения:
	// "nothing" - ничего не происходит, продолжаем играть. 
	// "civ" - город побеждает. 
	// "maf" - мафия побеждает.
	// "draw" - ничья. (По умолчанию).
	"no_kill3": "nothing" / "civ" / "maf" / "draw"

	// Третье переголосование. Предположим стол поделился (например при 6 игроках 1-2-3 за 4, 4-5-6 за 1). При переголосовании он опять поделился, но другими составами голосующих (например 1-2-5 за 4, 3-4-6 за 1).
	// Вопрос назначать еще одно переголосование, или считать, что уже поделили дважды и надо либо подымать, либо оставлять обоих. Значения:
	// true - да, дать еще по 30 секунд и еще раз проголосовать. Но после третьего уже точно спрашивать про подъем, даже если составы опять не совпадут. Иначе можно играть бесконечно.
	// false - нет. (По умолчанию).
	"revoting3": true / false
	
	// Должен ли последний убитый произнести речь, или сразу объявляется конец игры
	// true - да, должен.
	// false - нет не должан. (По умолчанию). 
	// "day" - только если убит днем.
	// "night" - только если убит ночью.
	"last_kill_speech": true / false / "day" / "night"
}

